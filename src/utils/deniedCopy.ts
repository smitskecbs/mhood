export function formatDeniedRequired(thresholdLabel: string): string {
  return `Required: ${thresholdLabel} MHOOD`;
}

export function formatDeniedBalance(balanceLabel: string): string {
  return `Your balance: ${balanceLabel} MHOOD`;
}
